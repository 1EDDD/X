# X

On-device iOS customization toolkit.

## Build

Generate an Xcode project with XcodeGen, then build X for a physical iOS device.

## Features

- On-device pairing interface
- SpringBoard tweak catalog
- Persistent tweak selections
- Property-list payload generation

System modifications are kept behind the device integration layer.
